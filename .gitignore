# Build / deploy artifacts (rebuilt on demand)
*.zip

# OS / editor cruft
.DS_Store
Thumbs.db
*~

# Dependencies (not used by the static site)
node_modules/
__pycache__/

# Claude Code worktrees (created by agent isolation mode)
.claude/worktrees/
